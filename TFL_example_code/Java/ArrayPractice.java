public class ArrayPractice {

    /**
     * Calculate and return the sum of all elements in the array.
     *
     * @param arr integer array
     * @return sum of all elements
     */
    public static int sumArray(int[] arr) {
        // TODO: Loop through each element in arr, summing all values.
        return 0; // placeholder
    }

    /**
     * Count occurrences of a specific character in a char array.
     *
     * @param arr character array
     * @param target character to count
     * @return count of target character
     */
    public static int countCharOccurrences(char[] arr, char target) {
        // TODO: Loop through arr, count how many times target appears.
        return 0; // placeholder
    }

    /**
     * Find and return the longest string in the array.
     *
     * @param arr string array
     * @return longest string
     */
    public static String findLongestString(String[] arr) {
        // TODO: Loop through arr, find and return the longest string.
        return ""; // placeholder
    }

    /**
     * Reverse an integer array in place.
     *
     * @param arr integer array to reverse
     */
    public static void reverseArray(int[] arr) {
        // TODO: Reverse arr in place (do not use additional arrays).
    }

    /**
     * Count words of a specified length in an array of strings.
     *
     * @param arr array of words
     * @param length specified word length to count
     * @return number of words of the specified length
     */
    public static int countWordsOfLength(String[] arr, int length) {
        // TODO: Loop through arr and count words with length matching the given length.
        return 0; // placeholder
    }

    /**
     * Convert a sentence to a char array and count vowels.
     *
     * @param sentence string to convert and count vowels in
     * @return number of vowels in sentence
     */
    public static int countVowels(String sentence) {
        // TODO: Convert sentence to a char array, count 'a', 'e', 'i', 'o', 'u'.
        return 0; // placeholder
    }

    /**
     * Find the minimum and maximum values in an integer array.
     *
     * @param arr integer array
     * @return array of two integers, {min, max}
     */
    public static int[] findMinAndMax(int[] arr) {
        // TODO: Find minimum and maximum values in arr, return as array {min, max}.
        return new int[]{0, 0}; // placeholder
    }

    /**
     * Count occurrences of each element in an integer array.
     *
     * @param arr integer array
     * @return a string showing each element and its occurrence count
     */
    public static String countElementOccurrences(int[] arr) {
        // TODO: Count occurrences of each element in arr and return a formatted string.
        return ""; // placeholder
    }
}

